{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "lerna run start --stream",
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test --stream --scope api"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
